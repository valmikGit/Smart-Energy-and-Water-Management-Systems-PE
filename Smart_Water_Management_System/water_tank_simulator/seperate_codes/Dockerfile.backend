# Dockerfile.backend
FROM python:3.11-slim

WORKDIR /app
COPY backend.py requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

# Create data directory volume mount in compose
VOLUME /app/data

EXPOSE 8000
CMD ["uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8000"]