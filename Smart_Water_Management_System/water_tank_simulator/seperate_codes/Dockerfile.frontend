# Dockerfile.frontend
FROM python:3.11-slim

WORKDIR /app
COPY streamlit_frontend.py requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8502
CMD ["streamlit", "run", "streamlit_frontend.py", "--server.port=8502", "--server.address=0.0.0.0"]